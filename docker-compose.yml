version: '3.7'
services:
  redis:
    extends:
      file: common-services.yml
      service: redis-base
  data-service:
    build:
      context: .
      dockerfile: ./apps/data-service/Dockerfile
    depends_on:
      - redis
    ports:
      - '${CRON_PORT}:${CRON_PORT}'
    restart: always
  api:
    build:
      context: .
      dockerfile: ./apps/api/Dockerfile
    ports:
      - '${API_PORT}:${API_PORT}'
    restart: always